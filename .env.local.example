# Vercel KV Credentials (für lokales Testen)
# Kopiere diese Werte aus deinem Vercel KV in deinem Vercel-Projekt
# (Vercel Dashboard → Storage → KV → Right-click "Connect" und wähle ".env.local")

KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Optional: Wenn du Upstash statt Vercel KV nutzt, kopiere die Werte hier stattdessen
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
